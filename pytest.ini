[pytest]
testpaths = tests
addopts = -q --strict-markers --timeout=1200
markers =
    emulator: needs the real ROM and boots PyBoy
    slow: takes more than a few seconds
    llm: needs a running Ollama daemon
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using SDL2 binaries.*:UserWarning
